import './setPublicPath';

// Style-only entry point.  Used as part of applicationEntry, but also usable standalone.

import(/* webpackChunkName: 'applicationStylesheet' */ '../styles/application.scss');
