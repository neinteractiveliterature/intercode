#import "./queries.gql"

mutation UpdateRootSite($defaultLayoutId: Int, $rootPageId: Int) {
  updateRootSite(input: { root_site: { default_layout_id: $defaultLayoutId, root_page_id: $rootPageId } }) {
    root_site {
      ...RootSiteFields
    }
  }
}
