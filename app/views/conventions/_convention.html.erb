<%= link_to convention.name, root_url(:host => convention.domain) %> /

<% unless convention.unspecified? %>
Dates: <% convention.start_date %> to <% convention.end_date %> /
<% end %>

<%= link_to 'Edit', edit_convention_path(convention) %>