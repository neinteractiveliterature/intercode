Signup changes for <%= @event.title %>

<%= @whodunit.name_without_nickname %> has edited the buckets for
<%= @event.title %>, which has caused <%= pluralize @move_results.size, 'signup' %>
to move:

<%= '=' * 80 %>

<% @runs.each do |run| -%>
<%= run_description(run) %>
<% @move_results_by_run[run].each do |move_result| -%>
  <%= move_result_description(move_result, show_buckets: true) %>
<% end -%>
<% end -%>
